#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <stdbool.h>
#include <unistd.h>

#include "GA.h"


int main(int varc, char* vars[]){

	// int data_length = 10000;
	// char* data = calloc(data_length, 1); 

	// int output_length = 10000;
	// char* output = calloc(output_length, 1); 


	// if (varc < 3){
	// 	printf("Invalid number of arguments!\n");
	// 	exit(1);
	// }

	// char* prog = vars[1];
	// char* input = vars[2];

	// // char* prog = "++++++++[>++++[>++>+++>+++>+<<<<-]>+>+>->>+[<]<-]>>.>---.+++++++..+++.>>.<-.<.+++.------.--------.>>+.>++.";
	// // char* input = "ABC";

	// // printf("%i\n",strcmp(prog_arg, prog));
	// // printf("> %s\n", prog_arg);
	// // printf("> %s\n", prog);

	// interpret(
	// 	prog, strlen(prog),
	// 	input, strlen(input),
	// 	data, data_length,
	// 	output, output_length
	// );

	// output[output_length-1] = '\0';

	// printf("%s", output);

	// free(data);
	// free(output);


	GA();
		

	return 0;
}